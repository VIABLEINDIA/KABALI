"""Cross-sectional (portfolio) research layer.

Distinct from `kabali.strategies`, which holds intraday time-series rules judged
per symbol. Nothing here trades; it exists to test whether a ranking built across
symbols carries an edge the per-symbol rules did not.
"""
